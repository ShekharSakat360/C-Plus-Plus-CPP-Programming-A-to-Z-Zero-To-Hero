#include <iostream>
using namespace std;

int main()
{
    int  x = 5; //x is a variable  
    //Here we are telling the compiler that allocate  4 bytes of memory
    //to hold integer value int x means we are telling the compiler
    //to hold integer value 
    cout << x << " ";
    int y = 10;
    cout << y << " ";//y is  a variables
    //Variables are used to store and access values
    //Variable can hold only one memory location at a time
    //Variable hold of a memory location of memory in RAM
    return 0;
}