#include <bits/stdc++.h>
using namespace std;

int main()
{
    const int x = 10;
    //When we declare a variable with const than that memory location value is marked as fixed value
    //Named Constant are the constant whose value remain fixed throughout the program execution
    //meaning the value do not change

    cout << x << endl;
    return 0;
}